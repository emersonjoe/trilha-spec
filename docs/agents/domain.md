# Domain Docs

This repository uses a single-context domain documentation layout.

Before exploring, read `CONTEXT.md` when present and the relevant decisions under `docs/adr/`. Missing files are not errors; create or extend them only when domain terminology or an architectural decision needs to be recorded.

Use the glossary vocabulary consistently in issues, tests, code and reviews. Surface conflicts with an existing ADR explicitly instead of silently overriding it.
