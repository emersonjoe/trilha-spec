# Written by trilha-spec init. Protocol files are committed; runner state is not.
runs/
cache/
app
app.exe
export-app
export-app.exe
# A private signing key is never committed; keygen writes it elsewhere.
keys/*.key
